{% extends 'base.html.twig' %}

{% block title %}Volunteering{% endblock %}

{% block body %}
    <div class="container flex-grow-1 container-p-y">
        <div class="row">
            <div class="col-md-8 m-auto">
                <div class="card mb-4">
                    <div class="card-body">
                        <div class="card-title">
                            <h1 class="fw-bold" style="color: limegreen;">{# TODO: Insert conference name #}</h1>
                        </div>
                    </div>
                    <ul class="list-group list-group-flush">
                        <li class="list-group-item">
                            <div class="d-flex justify-content-between mt-4">
                                <div>
                                    <p>{# TODO: Insert volunteering user's email #} volunteered from <span
                                            class="fw-semibold">{# TODO: Insert volunteering start date #}</span> to
                                        <span class="fw-semibold">{# TODO: Insert volunteering end date #}</span></p>
                                    <p class="my-0">Conference: <a href="{# TODO: Link to show Conference page #}">{# TODO: Insert conference name #}</a></p>
                                </div>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>

{% endblock %}
